# Agent Instructions

Use `AGENTS.md` as the canonical Codex agent instruction file for this repository.

This lowercase file exists because the requested artifact was named `agent.md`; the operative workflow, rules, and role definitions live in `AGENTS.md`.
